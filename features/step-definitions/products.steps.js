const { When, Then } = require('@cucumber/cucumber');

When('I click on the {string} link from the left menu', async function (linkName) {
});

Then('the main page should display available documents', async function () {
});

When('I click on the displayed document link', async function () {
});

Then('a new tab with the PDF content should open', async function () {
});